# Celestial Mobile

Capacitor hybrid app for iOS & Android.

## Quick Start

```bash
cd mobile/capacitor
npm install
# Copy desktop/celestial-dashboard.html to www/index.html
npx cap add ios
npx cap add android
npx cap sync
npx cap open ios   # or android
```

See vpn-proxy-control-example.js for sample native control bridge.
